#include <stdlib.h>
#include <stdio.h>
#include "ternary.h"

ternary toTernary(unsigned long input) {
  // UPDATE WITH YOUR CODE
  ternary b;
  return b;
}

unsigned long fromTernary(ternary b) {
  // UPDATE WITH YOUR CODE
  return 0;
}

ternary addTernary(ternary a, ternary b) {
  // UPDATE WITH YOUR CODE
  ternary res;
  return res;
}
